{
  "name": "weather-visual-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:deploy": "npm run build && npm run copy-assets",
    "copy-assets": "mkdir -p ../../public/weather-visual && cp -R dist/* ../../public/weather-visual/",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {
    "ogl": "^0.0.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
